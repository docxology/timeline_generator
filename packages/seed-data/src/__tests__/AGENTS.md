# AGENTS.md — seed-data/src/__tests__/

Tests import `PERSONS`, `EDGES`, `EVENTS` from `../` and validate referential integrity.
If adding new persons/edges/events, run `pnpm --filter seed-data test` to ensure integrity.
